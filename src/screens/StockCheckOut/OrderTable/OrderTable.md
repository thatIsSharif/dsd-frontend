# Overview
Wrapper component for 'Table' used in select driver screen as we can't use outlet context directly in table component.
This is not a pure component and is only made for select driver screen.

### Inclusion

```
import /component/OrderTable/OrderTable.tsx';
 
 <OrderTable />
```

### Use Case Description
1. Used to display assigned stock in van seller journey.

