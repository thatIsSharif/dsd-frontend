## Overview

The `SignOutSelect` component is designed to display logout button through a selector list. It provides a flexible and reusable structure for displaying logout selector on each screen.

### Inclusion

```
import SignOutSelect from 'component/SignOutSelect/SignOutSelect.tsx';

<SignOutSelect/>
```

### Use Case Description

1. Used to display a reusable logout selector.
